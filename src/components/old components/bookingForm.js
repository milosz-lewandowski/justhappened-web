import React from "react";

export class bookingForm extends React.Component {
    render() {
        return (
            <div>
                <Form schema={schema}>
                    <div>
                        <button type="submit">Submit</button>
                        <button type="button">Cancel</button>
                    </div>
                </Form>
                </div> //TODO: bookingForm
        )
    }
}